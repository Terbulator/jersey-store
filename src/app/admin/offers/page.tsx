import { AdminInProgress } from '@/components/admin/admin-in-progress';

export const metadata = { title: 'Offers — HEADERR Admin' };

export default function AdminMenuPage() {
  return <AdminInProgress title="Offers" description="Discount offers and deals." />;
}
