import { AdminInProgress } from '@/components/admin/admin-in-progress';

export const metadata = { title: 'Collections — HEADERR Admin' };

export default function AdminCollectionsPage() {
  return <AdminInProgress title="Collections" description="Manage collections and product groups." />;
}