import { AdminInProgress } from '@/components/admin/admin-in-progress';

export const metadata = { title: 'Categories — HEADERR Admin' };

export default function AdminCategoriesPage() {
  return <AdminInProgress title="Categories" description="Manage product categories and ordering." />;
}